<!-- Thanks for contributing! Keep the change focused. -->

## What & why

<!-- What does this change and why? Link any related issue. -->

## Testing

- [ ] `go build ./...`, `go vet ./...`, `gofmt -l internal cmd` (no output)
- [ ] `golangci-lint run ./...` — 0 issues
- [ ] `go test -race ./...` — green
- [ ] Added/adjusted a test that fails before and passes after this change

## Notes

<!-- If this touches code-generated files under internal/cli/, say so.
     Do not include API keys, device serials, user emails, or org IDs in this PR. -->
