version: 2
project_name: addigy-cli
changelog:
  disable: true
builds:
  - id: addigy-cli
    main: ./cmd/addigy-cli
    binary: addigy-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/joelcodess/addigy-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: addigy-mcp
    main: ./cmd/addigy-mcp
    binary: addigy-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
# Artifact signing scaffold — uncomment once a GPG key is provisioned:
#   1. Generate a release signing key: gpg --quick-generate-key "Addigy CLI Release <joel@addigy.com>" ed25519 sign
#   2. Export it and add as repo secret GPG_PRIVATE_KEY; import it in release.yml
#      before the goreleaser step, and set GPG_FINGERPRINT from the imported key.
#   3. Uncomment the block below. goreleaser will then sign checksums.txt,
#      letting users verify all artifacts via the signed checksum file.
# signs:
#   - artifacts: checksum
#     cmd: gpg
#     args:
#       - --batch
#       - -u
#       - "{{ .Env.GPG_FINGERPRINT }}"
#       - --output
#       - "${signature}"
#       - --detach-sign
#       - "${artifact}"
